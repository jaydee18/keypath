keypath_fetch.get:
  path: '/keypath-fetch'
  defaults:
    _title: 'Fetch degrees'
    _controller: '\Drupal\keypath_fetch\Controller\KeypathFetchController::build'
  requirements:
    _permission: 'access content'
